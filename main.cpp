#include <iostream>
#include "Deque.h"
#include "Timer.h"

int main()
{
/*	nstd::deque<bool> d;
	std::vector<bool> v;

	nstd::uint it(100Ui64);
	_NSTD_FOR_I(it) {
		bool b(rand() % 2);
		d.push_back(b);
		v.push_back(b);
	}
	_NSTD_FOR_I(it) {
		if (d[i] != v[i])
			std::cout << "Err: " << i << ", ";
	}*/

	std::cout << _HIGH_BIT(nstd::uint);
	

	std::cout << '\n';
	std::system("pause");
}